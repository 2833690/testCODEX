# Стратегии

- Trend Following Breakout (`breakout`)
- Mean Reversion (`mean_reversion`)
- Volatility Breakout (`volatility_breakout`)
- Regime Filter (`regime_filter`)

Каждая стратегия изолирована, имеет параметры и может участвовать в optimization.
