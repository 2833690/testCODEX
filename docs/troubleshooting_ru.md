# Troubleshooting

- Ошибка импорта CSV: проверьте колонки `timestamp,open,high,low,close,volume`.
- Пустые сигналы: увеличьте окно данных.
- Нет графиков: проверьте, что есть сохранённые backtest runs в `storage/runs.jsonl`.
